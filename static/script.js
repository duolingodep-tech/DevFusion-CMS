window.addEventListener("load",()=>{setTimeout(()=>document.getElementById("intro")?.classList.add("hide"),1100)});
document.querySelectorAll(".reveal").forEach(el=>{const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add("visible")}),{threshold:.12});o.observe(el)});
const c=document.getElementById("bg"),x=c.getContext("2d");let p=[];function rs(){c.width=innerWidth;c.height=innerHeight}rs();addEventListener("resize",rs);
function make(){p=Array.from({length:Math.min(140,Math.floor(innerWidth/12))},()=>({x:Math.random()*c.width,y:Math.random()*c.height,s:Math.random()*2+.5,vx:(Math.random()-.5)*.45,vy:(Math.random()-.5)*.45}))}make();
function shapeMariana(t){let cx=c.width*.55+Math.sin(t/900)*130,cy=c.height*.42+Math.cos(t/1200)*80;let pts=[];for(let i=0;i<34;i++){let a=i/34*Math.PI*2;let r= i<17? 120*(1-i/30):70;pts.push({x:cx+Math.cos(a)*r*1.7,y:cy+Math.sin(a)*r*.45})}pts.push({x:cx-235,y:cy-55},{x:cx-235,y:cy+55},{x:cx+40,y:cy-70});x.globalAlpha=.55;x.strokeStyle="#00e1ff";x.fillStyle="#00e1ff";for(const q of pts){x.beginPath();x.arc(q.x,q.y,2,0,Math.PI*2);x.fill()}for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){let dx=pts[i].x-pts[j].x,dy=pts[i].y-pts[j].y,d=Math.hypot(dx,dy);if(d<90){x.globalAlpha=(90-d)/180;x.beginPath();x.moveTo(pts[i].x,pts[i].y);x.lineTo(pts[j].x,pts[j].y);x.stroke()}}}
function anim(t){x.clearRect(0,0,c.width,c.height);for(const q of p){q.x+=q.vx;q.y+=q.vy;if(q.x<0||q.x>c.width)q.vx*=-1;if(q.y<0||q.y>c.height)q.vy*=-1;x.globalAlpha=.78;x.fillStyle=getComputedStyle(document.body).getPropertyValue("--accent2")||"#8eeeff";x.beginPath();x.arc(q.x,q.y,q.s,0,Math.PI*2);x.fill()}for(let i=0;i<p.length;i++)for(let j=i+1;j<p.length;j++){let A=p[i],B=p[j],d=Math.hypot(A.x-B.x,A.y-B.y);if(d<105){x.globalAlpha=(105-d)/520;x.strokeStyle=getComputedStyle(document.body).getPropertyValue("--accent")||"#7c5cff";x.beginPath();x.moveTo(A.x,A.y);x.lineTo(B.x,B.y);x.stroke()}}if(document.body.classList.contains("theme-mariana"))shapeMariana(t);requestAnimationFrame(anim)}anim(0);
